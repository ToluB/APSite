<section id="convos">
<%= render 'convos/convo', :convo => @convo %>
</section>

<section id="posts">
	<%= nested_posts @posts.arrange(:order => :created_at) %> 

</section>

<div id="post_form">
<%= render 'new' %>
</div>

<%= will_paginate %>