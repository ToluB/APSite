<%= render 'user_nav', :user => @user %>

<% if @user.avatar.present? %>
	<%= image_tag @user.avatar, :class => "user_image" %>
<% elsif @user.pic_url.present? %>
	<%= image_tag @user.pic_url, :class => "user_image" %><br />
<% else %>
	<%= image_tag "no-image.png" , :class => "user_image" %><br />
<% end %>

<% if current_user == @user %>
<%= link_to "Upload picture", avatar_user_url(current_user.id) %>
<%#= button_to "Remove avatar", :onclick => current_user.avatar.remove! %>
<% else %>

Send private message (telephone icon)

<% end %>


<h3> Summary </h3>
<% if current_user == @user%>
	You have:<br />
<% else %>
	<%= @user.name %> has:<br />
<% end %>
<ul>
	<li> started <%= @user.convos.count %> conversations and made <%= @user.posts.count%> posts.</li>
	<li> earned <%= @user.merits %> merits.</li>
			<em> <%= link_to "Earn more merits!" , '#' %> </em>
	<li> logged in <%= @user.sign_in_count %> times</li>	
</ul>


<% if current_user == @user%>
	You are interested in:<br />
<% else %>
	<%= @user.name %> is interested in:<br />
<% end %>

<h3> <%= link_to "user.exams.count", "#" %> exams</h3>

<h3> Your Friends </h3>

You are <%= link_to "following", "#" %> users <br />
You are being <%= link_to "followed by", "#" %> users

<h3> Your Groups</h3>


Colleges and exams (private)
<% if current_user == @user%>
Private area (other users cannot see this) -- dropdown using jscipt?
<!-- Figure out how to checkbox for making this public; jscript for if checked, hide -->
You are interested in <%link_to "@user.colleges", "#"%> colleges
	
<% end %>