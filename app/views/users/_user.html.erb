<div class="user" data-post-id="<%= user.id %>">

	<%= user.name %>
	<%= render 'shared/merits', :item => user %>
	<!-- Add everything else about users here -->

<!-- User box height/width to be set by users css -->

</div>