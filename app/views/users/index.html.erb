<h1>Users</h1>

<!-- <%#= link_to "Upload avatar", user_avatar_url(:user_id => current_user.id)%><br>

<%# @users.each do |user|%>
	<%#=image_tag user.avatar%>
	<%# end %>
	
	<%#= if user_signed_in? %>
		<section>
       <%#= render 'shared/stats' %>
     </section>

<%#= render 'track_form' if signed_in? %>

<%# end %> -->