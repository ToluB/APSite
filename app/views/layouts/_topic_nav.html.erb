<br/>
<ul class="breadcrumb topics">
	<% subject.topics.each do |topic|%>
		<li>
			<%=link_to topic.name, convos_url(:topic_id => topic.id), :class => is_active?(topic.id) %><span class="divider">/</span>
	  </li>
	<% end %>
</ul>